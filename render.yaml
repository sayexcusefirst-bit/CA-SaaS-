services:
  - type: web
    name: taxai-ca-saas
    env: python
    buildCommand: "pip install -r requirements.txt"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    startCommand: "gunicorn main:app"

    plan: free
